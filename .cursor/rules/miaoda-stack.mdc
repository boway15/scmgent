---
description: >-
  【已归档】飞书妙搭 ZIP/CJS 双轨历史规范。默认开发请忽略本规则。
  仅当用户明确要求妙搭/ZIP/hono-app/ScmHonoModule 时参考。
globs:
  - apps/web/miaoda/**
  - apps/web/scripts/*miaoda*
  - docs/miaoda*
alwaysApply: false
---

# 妙搭对齐技术栈（已归档）

> **状态：已停用（2026-07-24）**  
> 生产与日常开发以 **自建 Docker** 为准：`apps/web/server`（ESM）+ `docker compose`。  
> 发布 SOP：`docs/local-server-release-sop.md`  
> 决策：`docs/superpowers/specs/2026-07-24-de-miaoda-docs-baseline-design.md`  
>  
> **Agent 默认行为**：新功能不要做妙搭兼容、不要跑 `pnpm zip:miaoda`、不要迁 `server/hono-app`。  
> 仅当用户明确要求「妙搭 / ZIP 导入」时，才使用下文历史说明。

---

以下内容为历史参考（ZIP → Nest 外壳 → Hono CJS 桥接）。

## 部署避坑（按优先级）

导入后「服务不可用 / API 失败」**多数是平台编译与挂载问题**，不是业务逻辑坏了：

| 优先级 | 现象 | 根因 | 处理 |
|:------:|------|------|------|
| P0 | `/api/auth/config` 失败，响应为 **HTML** | ViewModule 先于 Hono；或未注册 ScmHonoModule | `app.module.ts`：ScmHonoModule **在 ViewModule 之前** |
| P0 | 构建无 `SCM Hono mounted` | 业务在 `source_package/`，未参与 nest build | 迁入 **`server/hono-app/`** |
| P0 | `Hono entry not found` | hono-app 不在 dist | 确认 `server/hono-app/index.ts` 被编译 |
| P0 | `import.meta is not defined` | ESM 语法 + CJS 产物 | hono-app 删 `import.meta.url`，用 `__dirname` |
| P0 | `@scm/db` 解析失败 | 根 `packages/db/` 缺失 | 复制到根目录，`npm install` |
| P0 | API URL 无 `/app/app_xxx` | 前端子路径 | `apiUrl()` + vite `base` + Router basename |
| P1 | 「服务不可用」文案 | config 请求非 JSON | 修上列 P0 |
| P1 | 侧边栏空 | 未 seed | `miaoda-init-all.sql` |
| P2 | 地址栏 `/api/*` → CSRF 403 | 平台网关 | 应用内 F12 |
| P2 | `SERVE_STATIC=true` | 反模式 | 妙搭 **`false`** |
| P2 | `AUTH_DEV_MOD` | 拼写错 | `AUTH_DEV_MODE` |

**响应诊断**：HTML → View fallback；NestJS `Cannot GET` → hono 加载失败；`{ "message": "Not Found" }` → Hono 已挂载。

**已建项目不能重新导入 ZIP** → 在线改 `server/hono-app/` + `client/` + `app.module.ts`，或新建应用导入。

## 固定选型（历史）

- 前端：React 18 + TypeScript + Vite + Tailwind CSS + shadcn/ui
- 后端：Node.js + Hono + TypeScript（妙搭：`server/hono-app/`）
- 数据库：PostgreSQL + Drizzle ORM（`packages/db`）
- 包管理：pnpm（本地）/ npm（妙搭 ZIP）

## 妙搭运行形态（历史）

| 本地开发 | 妙搭生产 |
|----------|----------|
| `apps/web/server/index.ts`（ESM，tsx） | **`server/hono-app/index.ts`**（CJS，nest build） |
| `apps/web/src/` | `client/` |
| 无 NestJS | `server/main.ts`（**禁止修改**） |
| — | `server/app.module.ts`（注册 ScmHonoModule，**在 ViewModule 前**） |
| — | `server/modules/scm-hono/scm-hono.module.ts` |

## 导入与打包（历史）

- `pnpm zip:miaoda` → `apps/web/scm-agent-miaoda.zip`
- 脚本：`apps/web/scripts/zip-for-miaoda.js`、`miaoda-cjs-transform.js`
- 现场文档：`apps/web/miaoda/MIAODA-SETUP.md`、`hono-app-checklist.md`
- 发布 SOP：`docs/miaoda-new-app-release.md`

## 当前自建栈（默认）

- API：`apps/web/server/routes/`
- 定时任务实现：`apps/web/server/tasks/*.ts`（由外部 HTTP 触发，Header `X-Cron-Secret`）
- 发布：`docs/local-server-release-sop.md`
