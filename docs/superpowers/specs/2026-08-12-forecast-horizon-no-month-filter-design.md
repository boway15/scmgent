# 销售预测数据明细去掉月数筛选

## 背景

版本详情「数据明细」中的「未来 / 历史 N 个月」下拉与创建时月数脱钩，易与页头「共 N 个月」矛盾。

## 决策（已确认）

- 去掉月数下拉（未来 / 历史 / 明细均不再可选）
- 未来列：不传 `monthCount`，由后端按版本落库地平线展示
- 历史列：固定 12 个月
- 导出宽表 / 明细与上述规则一致
- 保留「未来 / 历史 / 明细」切换与导出按钮

## 范围

仅 `ForecastHorizonPanel`（及必要时清理无用 import）。不改创建月数、策略趋势区、SKU 抽屉。
